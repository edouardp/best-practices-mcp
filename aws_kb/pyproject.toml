[project]
name = "aws-kb-mcp"
version = "1.0.0"
description = "AWS Knowledge Base MCP Server for SDLC Documentation"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.0.0,<4",
    "boto3>=1.34.0",
]
